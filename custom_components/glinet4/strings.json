{
  "title": "GL.iNet (glinet4)",
  "config": {
    "step": {
      "user": {
        "description": "Set required parameters to connect to your router",
        "data": {
          "username": "[%key:common::config_flow::data::username%]",
          "host": "[%key:common::config_flow::data::host%]",
          "password": "[%key:common::config_flow::data::password%]",
          "consider_home": "Consider Home"
        },
        "data_description": {
          "username": "This is always 'root' by default on GL-iNet devices",
          "host": "The hostname or IP address of your GL-iNet router.",
          "password": "The password you use to login to your router's admin page, this is unlikely to be the same as your WiFi password",
          "consider_home": "Seconds to wait before deciding that a device is away"
        }
      },
      "reauth_confirm": {
        "title": "Re-authenticate with your GL-iNet router",
        "description": "The stored credentials for your GL-iNet router were rejected. Enter the current password to reconnect.",
        "data": {
          "username": "[%key:common::config_flow::data::username%]",
          "password": "[%key:common::config_flow::data::password%]"
        },
        "data_description": {
          "username": "This is always 'root' by default on GL-iNet devices",
          "password": "The password you use to login to your router's admin page, this is unlikely to be the same as your WiFi password"
        }
      },
      "reconfigure": {
        "title": "Reconfigure your GL.iNet router",
        "description": "Update the connection details for your router. It must be the same device (same MAC address).",
        "data": {
          "username": "[%key:common::config_flow::data::username%]",
          "host": "[%key:common::config_flow::data::host%]",
          "password": "[%key:common::config_flow::data::password%]"
        },
        "data_description": {
          "username": "This is always 'root' by default on GL-iNet devices",
          "host": "The hostname or IP address of your GL-iNet router.",
          "password": "The password you use to login to your router's admin page, this is unlikely to be the same as your WiFi password"
        }
      }
    },
    "error": {
      "cannot_connect": "[%key:common::config_flow::error::cannot_connect%]",
      "invalid_auth": "[%key:common::config_flow::error::invalid_auth%]",
      "unknown": "[%key:common::config_flow::error::unknown%]"
    },
    "abort": {
      "already_configured": "[%key:common::config_flow::abort::already_configured_device%]",
      "reauth_successful": "[%key:common::config_flow::abort::reauth_successful%]",
      "reconfigure_successful": "[%key:common::config_flow::abort::reconfigure_successful%]",
      "unique_id_mismatch": "This router reports a different MAC address, so it is not the device you are reconfiguring."
    }
  },
  "options": {
    "step": {
      "init": {
        "data": {
          "username": "[%key:common::config_flow::data::username%]",
          "host": "[%key:common::config_flow::data::host%]",
          "password": "[%key:common::config_flow::data::password%]",
          "consider_home": "Consider Home"
        },
        "data_description": {
          "username": "This is always 'root' by default on GL-iNet devices",
          "host": "The hostname or IP address of your GL-iNet router.",
          "password": "The password you use to login to your router's admin page, this is unlikely to be the same as your WiFi password",
          "consider_home": "Seconds to wait before deciding that a device is away"
        }
      }
    }
  },
  "issues": {
    "statistics_not_collecting": {
      "title": "Flow statistics are not collecting per-app data",
      "description": "Flow statistics are enabled on {device}, but NAT acceleration is turned off, so per-application data is not being recorded.\n\nNAT acceleration and QoS/SQM (Smart Queue Management) cannot run at the same time on this router. To collect per-application statistics, disable QoS/SQM on the router's admin page. To keep QoS/SQM, turn off the Flow statistics switch to dismiss this notice."
    },
    "tailscale_reauth_required": {
      "title": "Tailscale needs re-authentication",
      "description": "Tailscale on {device} has dropped out of your tailnet and needs to be signed in again. This can happen when the Tailscale switch is toggled, because the router re-runs `tailscale up --reset`.\n\nOpen the router's Tailscale page (or the Tailscale status sensor's `auth_url` attribute) and sign in to reconnect. This notice clears automatically once the router is connected."
    },
    "router_mode": {
      "title": "Router is not in router mode",
      "description": "{device} is currently in {mode} mode. Tailscale and VPN features require the router to be in router mode and are unavailable in the current mode.\n\nSwitch the device to router mode on its admin page to use these features, or ignore this notice if you intend to run it in {mode} mode."
    }
  },
  "exceptions": {
    "action_failed": {
      "message": "Could not apply the change to {device}. The router reported: {error}"
    }
  }
}
